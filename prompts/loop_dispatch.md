Every implementer subagent receives EXACTLY this, nothing more:

1. Its phase section from the plan, verbatim: {phase_section}
2. The covered AC texts, verbatim Given/When/Then: {covered_acs}
3. The spec sections the phase's notes cite: {spec_sections}
4. Relevant lessons from `lessons.md` and full `memory.md`.
5. The report contract (attached).

Never rely on the run history, never read other phases' sections, never read the whole spec.
